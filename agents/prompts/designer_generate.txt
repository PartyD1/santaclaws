Generate a polished single-file HTML website mockup for the business below.
This is for a live hackathon demo, so the page must look like something a
local business owner would proudly click through on a phone.

Business:
- Name: {business_name}
- Type: {niche}
- City: {city}
- Address: {address}
- Phone: {phone}
- Hours: {hours}
- Google rating: {rating} ({review_count} reviews)
- Customer pain points to address: {pain_points}

Design variant: {variant_name}
Variant style guide:
{variant_style_block}

Requirements:
- Output ONE complete HTML file with Tailwind via CDN.
- Mobile-responsive, especially around iPhone 12 width.
- Above the fold must show business name, service type, city, trust cue, and one dominant CTA: "Call {phone}" as a tel: link.
- Include a secondary appointment/contact CTA only if it does not compete with the phone CTA.
- Sections in order: hero, quick trust/service strip, services, customer pain fixes, about/local trust, hours and location, contact.
- Use the actual business name, phone, city, and address when present.
- Make the page feel like a real local business, not a SaaS landing page: practical service copy, neighborhood trust language, and clear repair/service categories.
- Use restrained spacing, readable type, strong contrast, and polished cards or bands. Avoid giant empty hero areas.
- Make the hero feel designed: a compact nav/contact line, a clear headline, one useful paragraph, a CTA row, and 2-3 small trust/service badges.
- Add visual hierarchy with subtle borders, section bands, small labels, service cards, and one standout contact panel.
- Use realistic auto-shop copy: diagnostics, brakes, maintenance, check engine lights, fluids, tires, pre-trip inspections, appointment scheduling.
- Use customer pain points as "what we fixed on the new site", not as negative public accusations.
- Keep buttons, cards, and panels at 8px radius or less.
- No lorem ipsum.
- No stock-image URLs. Use Tailwind colors, simple texture via CSS classes, panels, and typography.
- Avoid generic phrases like "solutions", "elevate", "experience excellence", and "unlock your potential".
- Do not invent certifications, awards, warranties, staff names, prices, or hours not provided.
- No JavaScript required.
- Do not include explanatory text saying this is a mockup, demo, fallback, AI-generated, or generated by Designer.
- Return a complete document that looks good even if opened directly in a browser.

{previous_critique_block}

Return ONLY JSON:
{{
  "html": "<!DOCTYPE html>..."
}}
