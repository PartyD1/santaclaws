# Mainstreet Dashboard

Next.js 14 App Router shell for the demo dashboard.

Setup:

1. Run `npm install` from `dashboard/`.
2. Run `npm run dev` for local development.
3. Task 22 connects the shell to Supabase data.

No auth, billing, settings pages, queues, or production abstractions belong here for the hackathon build.
