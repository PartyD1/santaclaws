# NemoClaw Setup

Task 0 will replace these notes with the exact commands that work in the real sandbox.

Expected flow:

1. Install NemoClaw using the official installer or Brev early-preview flow.
2. Run `nemoclaw onboard` for sandbox `mainstreet`.
3. Configure the Nemotron provider route.
4. Verify `nemoclaw mainstreet status`.
5. Connect with `nemoclaw mainstreet connect`.
6. From inside the sandbox, smoke-test Nemotron and Supabase.

For the demo story, describe the runtime as: NemoClaw gives us secure always-on claws, Supabase gives them shared memory, and Nemotron gives them reasoning.
